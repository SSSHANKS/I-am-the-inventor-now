# Copy to .env and fill in. Never commit the real .env - it is gitignored.
#
# Credentials are read from the environment by litellm inside AgentProvider, so the
# variable name is the provider's, not ours. Which model each agent uses is configured
# separately, in config/agents_config.json.

GEMINI_API_KEY=
