# IATIN — Dirty → Border pipeline. Install into a project-local .venv:
#   python -m venv .venv && .venv/Scripts/activate
#   pip install -r requirements.txt
#   pip install -e ../Agent-Provider

# Artifact schema validation. Target the 4.x line; the legacy tree was written
# against 3.x semantics and its schemas are exercised against 4.x in tests/.
marshmallow>=4.0,<5.0

# Loads the local .env so litellm (underneath AgentProvider) can read provider keys.
python-dotenv>=1.0

# --- Dev ---
pytest>=8.0
ruff>=0.6

# --- Model access: AgentProvider (sibling repo at ../Agent-Provider) ---
# Install separately (path differs per machine; editable keeps both repos in sync):
#   pip install -e ../Agent-Provider
